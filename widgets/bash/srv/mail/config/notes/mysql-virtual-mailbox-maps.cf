# MySQL connection parameters
user = postfix_user
password = postfix_password
hosts = localhost
dbname = postfix_database

# Query to retrieve virtual mailbox mappings
query = SELECT mailbox FROM virtual_mailboxes WHERE email='%s'
