#!/bin/bash
# SCRIPT_DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )"
# SCRIPT_DIR="${SCRIPT_DIR/bash\/dt/bash}"
# source  "$SCRIPT_DIR/load-vars.sh"
watch -n 30 $widgets/widgets/bash/nav/p.sh  secure_vnc
