#!/usr/bin/python3
from os import sep; from pathlib import Path; loader=''; _='';_v='';
with open(str(Path.home())+sep+'.rt'+sep+ 'micro.py', 'r') as file: exec(file.read()); exec(loader);

########################################################################################
#n)--> start

for path in _.fo(): _.pr(path)