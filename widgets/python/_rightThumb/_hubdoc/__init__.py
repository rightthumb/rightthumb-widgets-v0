#!/usr/bin/python3

# ## {R2D2919B742E} ##
# ###########################################################################
# What if magic existed?
# What if a place existed where your every thought and dream come to life.
# There is only one catch: it has to be written down.
# Such a place exists, it is called programming.
#    - Scott Taylor Reph, RightThumb.com
# ###########################################################################
# ## {C3P0D40fAe8B} ##

__________________________________________________________________________________

github rightthumb



from rightthumb import app
from rightthumb import func as _
reg = app.space(__name__, __file__)
reg.documentation = {
}

reg.rent = app.register(reg)
app.rent = app.focus(reg.rent)
app.switches()
app.tables()
app.switch.reg( '' )

app.documentation()
o = doc.info
r = doc.frame
a=app
a.a = app.architecture
a.f = app.fi
a.s=app.switch
alias a.s=app.switch

def sw():
	app.switch.reg( 'Name', '-n', 'description', req=1 )


def xyz:
	a.a( o(r()), fn=' description '  )
	if app.switch('Files').status:
		pass
	if a.s('Files').s:
	__.softExit()

if a.f() and ( True ) and a.a( o(r()), fi=' description '  ):


if __name__ == '__main__':
	action()
	__.isExit(diagram=False)

app architecture diagram

app.threads()


__________________________________________________________________________________

profile root
rt.load.sh or bat
and rt.py

eval('''
from rightthumb import _'''+argv[0]+'''
_'''+argv[0]+'''.sw()
_'''+argv[0]+'''.run()

''')


website gears then github

bm -save
bm -go

# *glance at how to do github readme so i can take notes as i go

__________________________________________________________________________________
epyi hub -file imports.txt

epyi hub -file app
epyi hub -file _func
epyi hub -file _switches
epyi hub -file _tables
epyi hub -file _terminal
epyi hub -file _json
epyi hub -file _jsonfix
epyi hub -file _imp

$( rightthumb.terminal )

__________________________________________________________________________________




