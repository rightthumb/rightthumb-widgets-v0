

# ## {R2D2919B742E} ##
# ###########################################################################
# What if magic existed?
# What if a place existed where your every thought and dream come to life.
# There is only one catch: it has to be written down.
# Such a place exists, it is called programming.
#    - Scott Taylor Reph, RightThumb.com
# ###########################################################################
# ## {C3P0D40fAe8B} ##

strategy
	
	if charID processCharID



actionPlan( p )
				omit



testRule( p, r )
processCharID( p, charRules )








charRules['languages']

characters
			id
			char
			print
			[database]
			[languages]
						code 'global', 'python'
						groupID False 12
						[is] ['inline comment','comment','end','quote']
						[set] [6,12]
						nest 0 1
						isOpen 0 1
						hasClose 0 1


Truisms
		- isOpen = 0, irrelevant unless looking up an id
		- groupID = bool, 
		- hasClose is irrelevant if isOpen = 0


Processes
			- force beggining of line, ? always
		



INLINE COMMENT - Single Char
	rl['isOpen'] == 1 and rl['hasClose'] == 0 and type( groupID ) == bool

	rl['isOpen'] == 1 and rl['hasClose'] == 0 and type( groupID ) == bool



ONE
	STATUS ###############################################################      DONE
	isOpen = 1
	hasClose = 0
	type( groupID )
	# INLINE COMMENT - TO END OF LINE 
	while not inDatabaseByCategory( 'end', pt )

TWO
	STATUS ###############################################################      ____
	isOpen = 1






TODO DUMP
	
	- fail properly
	- document fail
	- document success






